const colors = {
  gray: "#c0c0c0",
  black: "#333333",
  whiteDirty: "#F6F6F6"
};

const transition = "all 0.2s ease-in-out";

const breakpoints = {
  sm: 320,
  md: 600
};

const theme = {
  colors,
  transition,
  breakpoints
};

export default theme;
