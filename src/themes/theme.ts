const colors = {
  gray: "#C0C0C0",
  grayDark: "#666",
  black: "#333",
  whiteDirty: "#F6F6F6",
  turquise: "#00ACC1",
  white: "FFF"
};

const transition = "all 0.2s ease-in-out";

const breakpoints = {
  mobile: "425px"
};

const theme = {
  colors,
  transition,
  breakpoints
};

export default theme;
